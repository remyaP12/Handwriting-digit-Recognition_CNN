# Handwriting-digit-Recognition_CNN

A CNN model is used to recognize handwritten digits (0–9) by learning image features like edges and shapes automatically.
It processes images through convolution and pooling layers to accurately classify each digit.
